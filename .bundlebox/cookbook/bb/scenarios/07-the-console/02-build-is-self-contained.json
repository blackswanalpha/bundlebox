{
 "id": "cc-build",
 "surface": "the-console",
 "severity": "medium",
 "title": "the page can be written as one file that opens with no server",
 "question": "does `bb console build` embed the state?",
 "rule": [
  "README: `bb console build` writes the same page as a single file with the state embedded."
 ],
 "steps": [
  {
   "name": "it writes a file",
   "run": "node bin/bb.js console build",
   "expect": {
    "rc": 0,
    "stdout_contains": "index.html"
   }
  },
  {
   "name": "the state is in the file, not fetched",
   "static": {
    "file": ".bundlebox/out/console/index.html",
    "contains": "__STATE__"
   }
  },
  {
   "name": "and the measured savings are on the page",
   "static": {
    "file": ".bundlebox/out/console/index.html",
    "matches": "saved_pct"
   }
  },
  {
   "name": "state prints as data",
   "run": "node bin/bb.js console state --json | head -c 200",
   "expect": {
    "rc": 0,
    "stdout_contains": "workspace"
   }
  }
 ]
}